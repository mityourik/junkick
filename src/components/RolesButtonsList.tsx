import { RoleButton } from './RoleButton';

export const RoleButtonsList = () => {
  return (
    <div className="roles-buttons-list">
      <p className="roles-buttons-list__title">Я:</p>
      <RoleButton />
    </div>
  );
};
